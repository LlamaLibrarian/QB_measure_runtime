# QB_measure_runtime

Run this program to measure (and compare) the total runtime (in seconds) of 2 or more QB code fragment(s).
The code that runs in the shorter runtime is the better code that we want.

Filename= RT.BAS
